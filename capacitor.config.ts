import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tuubodega-admin',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
